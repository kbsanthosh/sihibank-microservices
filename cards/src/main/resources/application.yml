server:
  port: 9000

spring:
  application:
    name: cards
  profiles:
    active: "prod"
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    #import:
    #  - "optional:configserver:http://localhost:8071"
    #import:
    #  - "configserver:http://localhost:8071"
    import: "configserver:${SPRING_CLOUD_CONFIG_URI:http://localhost:8071}"


management:
  endpoints:
    web:
      exposure:
        include:
         # - "refresh"
          - "*"
  health:
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true
  info:
    env:
      enabled: true
  endpoint:
    shutdown:
      access: unrestricted
    health:
      probes:
        enabled: true


eureka:
  instance:
    preferIpAddress: true
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/

info:
  app:
    name: "cards"
    description: "Sihi Bank Cards Application"
    version: "1.0.0"


logging:
  level:
    com:
      sihibank:
        cards: DEBUG




